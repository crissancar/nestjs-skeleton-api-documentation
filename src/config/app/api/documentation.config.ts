export const documentationConfig = {
	url: null as string,
	title: 'Skeleton API documentation',
	auth: {
		enabled: true,
		user: null as string,
		password: null as string,
	},
	logo: {
		url: null as string,
		href: null as string,
		altText: null as string,
	},
	favicon: null as string,
	fetch: {
		url: null as string,
		apiKey: null as string,
	},
};
